---
layout: layouts/page
eleventyExcludeFromCollections: true
---

h2(class="text-stone-600 font-medium") Posts and other half-baked thoughts
div(class="mt-4")
    each post in collections.posts.reverse()
        a(href=post.url, class="grid gap-1 group py-4")
            div(class="flex flex-row justify-between text-sm text-stone-500")
                p(class="whitespace-nowrap")= filters.readableDate(post.date).split(' ').slice(0, ).join(' ')
                div(class="flex items-center opacity-0 group-hover:opacity-100 group-hover:pr-0 pr-4 transition-all ease-in-out duration-300")
                    p Read more
                    include /icons/link-arrow.svg
            h3(class="leading-tight text-xl font-medium text-blue-900 group-hover:text-blue-950 transition-colors ease-in-out duration-300") #{post.data.title}
            p(class="text-stone-500")= post.data.subtitle
include /footer
