---
layout: layouts/page
eleventyExcludeFromCollections: true
---

div(class="animate-fade-in")
    h2(class="text-stone-400 font-medium") Posts and other half-baked thoughts
    div(class="mt-4")
        each post in collections.posts.reverse()
            a(href=post.url, class="grid gap-1 group py-4")
                div(class="flex flex-row justify-between text-sm")
                    p(class="whitespace-nowrap text-stone-400")= filters.readableDate(post.date).split(' ').slice(0, ).join(' ')
                    div(class="flex items-center opacity-0 group-hover:opacity-100 group-hover:pr-0 pr-4 transition-all ease-in-out duration-300")
                        p Read more
                        include /icons/link-arrow.svg
                h3(class="leading-tight text-lg font-medium text-[#3b82f6] group-hover:text-white transition-colors ease-in-out duration-300") #{post.data.title}
                p(class="text-stone-400")= post.data.subtitle
    include /footer
