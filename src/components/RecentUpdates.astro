---
import {UPDATES} from "@consts";
---

<div class="grid grid-cols-4 gap-x-2 gap-y-4">
    {UPDATES.map((UPDATE) => (
    <div class="shrink font-medium text-black/50 dark:text-white/50">{UPDATE.ACTION}</div>
    <div class="col-span-3">{UPDATE.TEXT}</div>
    ))}
</div>